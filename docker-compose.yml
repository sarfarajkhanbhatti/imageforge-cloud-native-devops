services:
  imageforge:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: imageforge

    ports:
      - "5000:5000"

    environment:
      AWS_REGION: ${AWS_REGION:-us-east-1}
      S3_BUCKET: ${S3_BUCKET}

    restart: unless-stopped